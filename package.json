{
  "name": "bimmerstein-bin-analyzer",
  "private": true,
  "version": "0.2.18",
  "description": "Open-source Windows ECU binary analysis and map editing app",
  "license": "GPL-3.0-or-later",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "eval": "pnpm --filter @binanalyzer/eval start",
    "dev": "pnpm --filter desktop tauri dev",
    "build": "pnpm -r --if-present build"
  }
}
